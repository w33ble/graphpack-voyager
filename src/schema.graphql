type Product {
  id: String
  sku: String
  manufacturer: String
  category: [ProductCategory]
}

type ProductCategory {
  name: String
}

type Query {
  product(id: String): Product
  products: [Product!]!
}